<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_field :date, id: :post_date %>
  </div>

  <div class="field">
    <%= form.label :rationale %>
    <%= form.text_area :rationale, id: :post_rationale %>
  </div>

  <div class="actions">
    <%= form.submit "Save" %>
  </div>
<% end %>
