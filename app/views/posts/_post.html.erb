<%= post.id %>
<%= post.date %>
<%= post.user.full_name %>
<%= truncate(post.rationale) %>
<%= link_to 'Edit', edit_post_path(post), id: "edit_#{post.id}"%>
<%= link_to 'Delete', post_path(post), method: :delete, id: "delete_post_#{post.id}_from_index", data: { confirm: 'Are you sue'}%>

<%= link_to 'New Post', new_post_path %>
